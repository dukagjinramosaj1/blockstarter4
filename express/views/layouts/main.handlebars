<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
    <title>Blockstarter4.0</title>
	<link rel="stylesheet" type="text/css" href="/css/custom.css"></link>
</head>
<body>

<div class="container">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button><a class="pull-left" href="/"><img src="/img/logo.png"></a></div>
            <div class="collapse navbar-collapse" id="navcol-1">
                <ul class="nav navbar-nav navbar-left">
                    <li><a href="/"><span class="glyphicon glyphicon-home" style="color:#3366ff"></span> Home</a></li>
                    {{#if success}}
                        <li><a href="/myprojects"><span class="glyphicon glyphicon-stats" style="color:#3366ff"></span> My Projects</a></li>
                        <li><a href="/myinvests"><span class="glyphicon glyphicon-bitcoin" style="color:#3366ff"></span> My Investments</a></li>
                    {{/if}}
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {{#if success}}
                        <li><a href="/logout"><span class="glyphicon glyphicon-user" style="color:#3366ff"></span> Logout </a></li>
                    {{else}}
                        <li><a href="/login"><span class="glyphicon glyphicon-user" style="color:#3366ff"></span> Login </a></li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>
</div>


<div class="container">
    {{#if error}}
        <p class="alert alert-warning">{{error}}</p>
    {{/if}}
    {{{body}}}
</div>

<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

</body>
</html>
